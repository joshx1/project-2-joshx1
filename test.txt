class PrintStuff<T extends Publisher> {
    ArrayList<T> stuff = new ArrayList<>();
    ExecutorService threadPool = Executors.newFixedThreadPool(30);

    public void publish(){
       for(T s : stuff){
           threadPool.execute(() -> new Thread(s.publish()));
       }
    }
}